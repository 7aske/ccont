#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <stdio.h>
#include "shortid.c"

unsigned long ipow(double, double);

unsigned long decode(char*, int);

char* encode(unsigned long, int);